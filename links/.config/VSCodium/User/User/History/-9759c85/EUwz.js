import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';
import { mdsx } from 'mdsx';
import { config } from './mdsvex.config.js';
import adapter mdsxConfig), vitePreprocess()],
  kit: {
    adapter: adapter()
  }
};

export default config;
